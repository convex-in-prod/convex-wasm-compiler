import { getIndexFields } from "convex-helpers/server/stream";

function reviewedHelper() {
  throw getIndexFields;
}

try {
  reviewedHelper();
} catch (escaped) {
  const helperMember = "prototype";
  escaped[helperMember];
}
